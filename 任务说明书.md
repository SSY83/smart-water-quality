# 基于轻量化深度学习的浙江河道水质智能监测与预警系统 — 任务说明书

## 一、项目概述

本项目构建一套面向浙江省主要河道（苕溪、钱塘江、西湖）的水质智能监测与多级预警系统，采用"边缘端实时推理 + 云端深度分析"的协同架构。

**边缘端**部署于 Raspberry Pi 4B / Jetson Nano 嵌入式设备，运行 TensorFlow Lite 量化后的 MobileNetV2（水质四分类）与 U-Net（污染区域分割）轻量化深度学习模型，对 OpenCV 采集的视频帧与 Modbus RTU 传感器数据执行并行推理与多源融合，通过 MQTT（QoS 1 / TLS）和 gRPC 协议上传至云端。网络中断时自动切换 SQLite 本地缓存，恢复后触发断点续传。

**云端**基于 Java 11 + Spring Boot 2.7 + MyBatis Plus 框架，接收边缘端数据后执行融合验证、滑动窗口趋势检测、历史基线 Z-score 对比、线性回归水质预测等二次分析，通过三级告警体系（轻度→入库 / 中度→+WebSocket / 重度→+短信）推送，同时提供 ECharts 可视化数据接口与 Vue 3 + Element Plus 单页应用前端。

**通信体系**涵盖 MQTT（通配符主题订阅，三类消息：影像分析/传感器数据/心跳）、gRPC 双向流（端口 9090，支持流式分析结果上报、告警 ACK、模型版本同步、健康检查）、WebSocket（JWT 认证实时告警推送）。

---

## 二、关键技术

### 2.1 轻量化深度学习推理（边缘端）

**模型选型**
: MobileNetV2 四分类 + U-Net 语义分割。MobileNetV2 输出 turbidity_level（0-清晰/1-轻度/2-中度/3-重度）与 pollution_probs（4 类污染物概率）；U-Net 输出 224×224 像素级污染区域掩码。

**模型加载**
: 单例模式 + 双重检查锁，优先加载 tflite_runtime 轻量包，回退完整 TensorFlow，支持多线程副本池。

**量化推理**
: INT8 量化感知输入处理，自动检测输入张量 dtype，按 scale/zero_point 参数转换浮点输入为 uint8。

**并行推理**
: 线程池并行提交 MobileNetV2 与 U-Net 推理任务，单模型推理超时 200ms。

**降级容错**
: 模型加载失败或推理异常时自动切换规则引擎模式（基于颜色直方图亮度均值判定），每 300 秒自动尝试模型重载恢复。

### 2.2 多源数据融合算法（边缘端）

输入：影像分析结果（turbidity_level、pollution_probs、confidence）+ 传感器读数（浊度 NTU、COD mg/L、pH）。输出：FusionResult（final_score、alert_level、confidence）。采用六步融合：

**步骤1 — 时间对齐**
: 在 500ms 时间窗口内寻找与影像时间戳最接近的传感器读数；无匹配时传感器权重置零，降级为纯影像模式。

**步骤2 — 传感器评分**（0-1 连续值）

- 浊度评分 = min(浊度/80, 1.0) × 0.5
- COD 评分 = min(COD/50, 1.0) × 0.3
- pH 评分 = min(abs(pH-7.0)/3.5, 1.0) × 0.2

**步骤3 — 影像评分**（0-1 连续值）

- 浑浊度等级映射 {0:0.0, 1:0.33, 2:0.67, 3:1.0} × 0.7
- max(污染物概率) × 0.3

**步骤4 — 加权融合**
: final_score = image_score × (1 - sensor_weight) + sensor_score × sensor_weight，默认权重 0.6 / 0.4。

**步骤5 — 置信度计算**
: 有传感器匹配：consistency = 1 - abs(image_score - sensor_score)；无匹配：consistency = 0.5。最终置信度 = consistency × confidence_penalty × image_confidence。

**步骤6 — 等级判定**

| final_score 范围 | 等级     |
| ---------------- | -------- |
| < 0.4            | 正常     |
| 0.4 ~ 0.7        | 轻度异常 |
| 0.7 ~ 0.9        | 中度异常 |
| ≥ 0.9            | 重度异常 |

**规则引擎降级**：当深度学习推理不可用时，基于阈值硬判定，置信度固定 0.6。

| 指标       | 轻度阈值 | 中度阈值       | 重度阈值 |
| ---------- | -------- | -------------- | -------- |
| 浊度 (NTU) | > 5      | > 30           | > 80     |
| COD (mg/L) | > 15     | > 30           | > 50     |
| pH         | —        | < 6.5 或 > 8.5 | —        |

### 2.3 边缘-云通信协议

**MQTT**（常规遥测 + 传感器数据 + 心跳）
: Eclipse Paho 客户端，QoS 1，TLS 加密。三类主题：`/wqi/{point_id}/image_analysis`、`/wqi/{point_id}/sensor_data`、`/wqi/{point_id}/heartbeat`。云端通配符订阅 `/wqi/+/...`。断线自动重连（10 秒间隔）。

**gRPC**（高优先级告警 ACK + 模型版本同步 + 健康检查）
: 端口 9090。双向流式 RPC `StreamAnalysisResults` 支持持续上报与云端指令下发。一元 RPC：`ReportAlert`（10s 超时）、`GetModelVersion`、`HealthCheck`。

**Protobuf**（gRPC 消息序列化）
: 消息类型：WaterQualityMessage（含 ImageAnalysis / SensorData / AlertData / HeartbeatData）、EdgeCommand、AlertRequest / Response、ModelVersionRequest / Response、HealthCheckRequest / Response。

**断点续传**
: 网络中断 → SQLite 本地缓存 → MQTT 重连回调触发补传（按时间戳升序）。连续 5 次失败自动暂停。snake_case → camelCase 键名映射。

### 2.4 云端智能分析

**融合验证**
: 接收边缘端上报的 image_score 和 sensor_score，云端按 image × 0.6 + sensor × 0.4 重新计算验证。权重可配置。

**异常等级判定**
: final_score < 0.4 → 正常 / < 0.7 → 轻度 / < 0.9 → 中度 / ≥ 0.9 → 重度。

**滑动窗口趋势检测**
: 将指定时间窗口（默认 60 分钟）内数据等分为前后两段，比较 final_score 均值变化率。> 0.5 快速恶化 / > 0.2 恶化 / < -0.3 改善 / 其他稳定。同时检测相邻点差值 > 0.3 的突变点。最少 4 条数据。

**历史基线对比**
: 以近 30 天数据计算 AVG + STDDEV 基线，Z-score 偏离度分析。> 3.0 critical / > 2.0 significant / > 1.0 slight。通过 SQL 聚合函数批量计算。

**水质预测**
: 取最近 2 小时数据，最小二乘线性回归拟合（斜率+截距），外推预测 future_score，R² = 1 - SSres/SStot。最少 5 条数据点。

**规则降级**
: 边缘端仅上报传感器数据时使用。评分公式：turbScore × 0.5 + codScore × 0.3 + phScore × 0.2。

### 2.5 告警推送体系

**三级推送渠道**

| 等级           | 推送方式                                 |
| -------------- | ---------------------------------------- |
| 轻度 (level=1) | 仅入库记录                               |
| 中度 (level=2) | 入库 + WebSocket 实时广播                |
| 重度 (level=3) | 入库 + WebSocket 广播 + 短信异步立即发送 |

**多参数联合告警**
: 浊度、COD、pH 分别独立评估异常等级。三参数同时超标 → 最高等级 +1（上限重度）。两参数同时达到中度以上 → 同样升级。

**告警升级策略**
: 同一监测点 30 分钟内 ≥ 5 条告警 → 升 2 级；≥ 3 条 → 升 1 级。上限重度。

**指数退避重试**
: 推送失败进入优先级重试队列。间隔 [1, 2, 4, 8, 16] 分钟，最大 5 次，超限标记 failed。

**WebSocket 实时推送**
: 端点 `/ws/alerts`，JWT 认证（URL 参数或子协议头）。JSON payload，全用户广播 + 定向推送。心跳：客户端 25s ping，服务端 60s 超时断连。

**短信推送**
: 多提供商架构（阿里云 + Mock）。手机号查找链：监测点联系人 → 系统管理员 → 默认号码。指数退避 + 熔断保护。

### 2.6 缓存防护体系

针对可视化查询接口的高并发场景，构建三层防护：

| 防护类型 | 方案                      | 参数                      |
| -------- | ------------------------- | ------------------------- |
| 缓存穿透 | 空值标记缓存              | TTL 60s                   |
| 缓存击穿 | 互斥锁 + 双重检查         | 每 key 独立 ReentrantLock |
| 缓存雪崩 | 随机 TTL 偏移             | 基础 300s ± 60s           |
| 缓存预热 | 手动/启动时预加载热点数据 | 最近 1 天                 |

缓存层级：Caffeine 本地缓存（1000 条，5 分钟）→ Redis 分布式缓存（条件装配，不可用时自动降级）→ MySQL。

### 2.7 安全防护体系

**身份认证**
: JWT HS256 签名，claims 包含 userId / username / role，24 小时有效期，支持滑动刷新。

**JWT 黑名单**
: 登出时 token 加入本地缓存黑名单（TTL = token 剩余有效期），最大容量 10000，每次请求拦截器校验。

**密码安全**
: BCrypt 加密存储，cost = 10。

**权限控制**
: RBAC 三级角色（admin / user / readonly），admin 拥有所有权限。用户-监测点多对多权限关联。

**暴力破解防护**
: 同一用户名 5 次登录失败 → 账号锁定 15 分钟。登录成功自动清除计数。

**接口限流**
: Guava RateLimiter 令牌桶算法。全局 200 req/s + 每用户 20 req/s + 登录 10 req/s per IP。超限返回 HTTP 429。

**SQL 注入防护**
: 请求参数正则检测 6 组注入模式（SELECT / INSERT / UPDATE / DELETE / DROP / UNION / OR 注入 / SQL 注释符）。命中返回 403。排除静态资源与 WebSocket 路径。

**XSS 防护**
: 正则检测 4 组模式（script 标签 / on* 事件 / javascript 伪协议 / iframe 标签）。非 GET 请求参数自动 HTML 实体编码（`& < > " '` 转义）。

**全链路追踪**
: 请求入口生成 UUID 注入 MDC + 响应头 X-Trace-Id。JSON 结构化日志自动包含 traceId。

### 2.8 性能与可靠性

**异步批量写入**
: LinkedBlockingQueue（容量 1000），后台线程消费：50 条或 200ms 触发 batchInsert。队列满丢弃并告警。

**线程池隔离**（4 个独立池）

| 池名称          | 核心/最大线程 | 队列容量 | 用途           |
| --------------- | ------------- | -------- | -------------- |
| smsExecutor     | 10 / 50       | 200      | 短信异步发送   |
| mqttExecutor    | 2 / 5         | 100      | MQTT 消息处理  |
| dbWriteExecutor | 5 / 10        | 500      | 数据库批量写入 |
| retryExecutor   | 2 / 5         | 100      | 告警重试       |

全部配置 awaitTermination 优雅关闭。

**数据库读写分离**
: AbstractRoutingDataSource + AOP 切面自动路由。select / get / query / find 开头 → 读库，其余 → 写库。功能条件装配（开关控制）。

**短信熔断器**
: 连续 5 次发送失败 → 熔断器打开 600 秒。超时后半开探测，成功恢复，失败重新熔断。

**结构化日志**
: Logback 三环境（local 控制台 / dev JSON 文件 / prod JSON + Filebeat → Elasticsearch）。支持大小+时间滚动，多行异常合并。

**优雅停机**
: 停止采集 → 内存数据刷入 SQLite → 发送离线通知 → 断开 MQTT/gRPC → 关闭线程池 → 关闭数据库。

---

## 三、功能需求

### 3.1 边缘端功能

**视频采集**
: OpenCV 异步捕获（生产者-消费者 deque 缓冲），224×224 预处理（BGR→RGB，归一化 [-1,1]），连续 5 帧失败判定摄像头离线。

**传感器读取**
: Modbus RTU 轮询 pH / 浊度(NTU) / COD(mg/L)。仿真模式兼容无硬件环境。量程异常回退上次有效值。

**深度学习推理**
: INT8 量化 TFLite 模型。MobileNetV2 输出四分类 + 四类污染概率（有机污染物/氮磷污染/油脂污染/微塑料），U-Net 输出分割掩码。

**多源融合分析**
: 六步融合流水线（时间对齐→双通道评分→加权融合→置信度→等级判定）。推理不可用时自动降级规则引擎。

**MQTT 数据上报**
: 告警 → `/wqi/{id}/image_analysis`，正常传感器 → `/wqi/{id}/sensor_data`，心跳 → `/wqi/{id}/heartbeat`。QoS 1。

**gRPC 高优先级告警**
: 等级 ≥ 2 通过 ReportAlert 发送，等待 ACK 确认。ACK 失败时 MQTT 兜底。

**模型版本同步**
: 每 30 分钟通过 GetModelVersion 查询云端最新版本，支持 OTA 更新。

**离线数据缓存**
: SQLite 本地存储，LRU 变种双区（热区 LRU 100 + 冷区 FIFO 500）。(point_id, timestamp) 唯一约束去重。中度及以上告警保护 7 天。

**断点续传**
: MQTT 重连回调触发，按时间戳升序批量补传。连续 5 次失败暂停。

**本地告警生成**
: 告警 ID 格式 `ALT-{点位ID}-{时间戳}-{计数器}`，三级颜色标识 + 处理建议模板。

**设备心跳**
: 每 30 秒上报心跳包（设备 ID + 时间戳 + 缓存队列长度），超时 90 秒判离线。

### 3.2 云端功能

#### 3.2.1 认证授权

- 登录：BCrypt 验证 → 签发 JWT（userId / username / role）→ Redis 缓存 session
- 登出：Token 加入黑名单即时失效，清除 Redis session
- 拦截：从 Authorization 头提取 Bearer Token，验证签名 + 黑名单检查

#### 3.2.2 数据接收与智能分析

| 接口                                   | 方法 | 说明                                        |
| -------------------------------------- | ---- | ------------------------------------------- |
| `/api/data/report`                     | POST | 接收边缘端上报。NORMAL 仅入库，异常触发告警 |
| `/api/data/heartbeat`                  | POST | 更新设备最后在线时间                        |
| `/api/data/analysis/trend/{pointId}`   | GET  | 滑动窗口趋势：方向/变化率/风险等级/突变点   |
| `/api/data/analysis/compare/{pointId}` | GET  | 历史基线对比：均值/标准差/Z-score/偏离等级  |
| `/api/data/analysis/predict/{pointId}` | GET  | 水质预测：预测分数/等级/R²/趋势方向         |
| `/api/data/analysis/verify-fusion`     | POST | 云端二次融合验证                            |

#### 3.2.3 告警管理

| 接口                            | 方法 | 说明                                   |
| ------------------------------- | ---- | -------------------------------------- |
| `/api/alerts/point/{pointId}`   | GET  | 按监测点分页查询告警                   |
| `/api/alerts/{alertId}/confirm` | POST | 确认告警                               |
| `/api/alerts/{alertId}/dismiss` | POST | 解除告警                               |
| `/api/alerts/evaluate-combined` | GET  | 三参数联合规则评估                     |
| `/api/alerts/statistics`        | POST | 多点聚合统计（总量/等级分布/类型分布） |
| `/api/alerts/stats/{pointId}`   | GET  | 单点统计（确认率/等级分布）            |
| `/api/alerts/pending-retry`     | GET  | 查询待重试告警                         |

#### 3.2.4 可视化数据服务

| 接口                              | 方法 | 说明                                 |
| --------------------------------- | ---- | ------------------------------------ |
| `/api/visualization/trend`        | POST | ECharts 趋势数据，含完整缓存防护     |
| `/api/visualization/heatmap`      | POST | 热力图聚合（平均浊度/COD/pH/异常分） |
| `/api/visualization/query`        | POST | 分页历史查询（≤30 天，≤500 条/页）   |
| `/api/visualization/cache-stats`  | GET  | 缓存状态/Key 数/TTL 配置             |
| `/api/visualization/cache-warmup` | POST | 手动预热指定监测点                   |

#### 3.2.5 系统监控

- `GET /api/system/dashboard`：堆内存（used/max）、线程（活跃/峰值/守护）、GC（次数/耗时）、WebSocket 连接数、运行时长
- Actuator：health / info / metrics / env / loggers / threaddump

#### 3.2.6 WebSocket 实时推送

- 端点：`ws://host:8080/ws/alerts?token=<JWT>`
- 认证：URL 参数或 Sec-WebSocket-Protocol 头
- 心跳：客户端 ping / 服务端 pong，30s 扫描 60s 超时
- 推送：全用户广播 + 定向推送
- 格式：`{"type":"alert","data":{"alertId":...,"pointName":...,"level":...,"type":...,"time":...}}`

#### 3.2.7 gRPC 服务

| RPC                   | 类型   | 说明                                       |
| --------------------- | ------ | ------------------------------------------ |
| StreamAnalysisResults | 双向流 | 接收 WaterQualityMessage，返回 EdgeCommand |
| ReportAlert           | 一元   | 高优先级告警 → ACK 确认                    |
| GetModelVersion       | 一元   | 查询最新模型版本与下载地址                 |
| HealthCheck           | 一元   | 设备健康状态上报 + 推荐心跳间隔            |

### 3.3 前端功能

Vue 3 + Element Plus + ECharts 5 单页应用，Spring Boot 直接服务静态资源 + SPA 路由转发。

| 页面       | 路由          | 核心功能                                   |
| ---------- | ------------- | ------------------------------------------ |
| 登录页     | `/`           | 用户名密码登录，JWT 自动存储               |
| 仪表盘     | dashboard     | 监测点统计卡片 + 系统资源仪表盘 + 最近告警 |
| 告警管理   | alerts        | 列表筛选/确认/解除 + WebSocket 实时弹窗    |
| 数据可视化 | visualization | 监测点选择 + ECharts 趋势曲线 + 热力图     |
| 监测点管理 | monitoring    | 列表展示（名称/坐标/设备ID/状态/电话）     |
| 系统监控   | system        | 堆内存仪表盘 + 线程折线图 + GC + 连接数    |
| 告警统计   | statistics    | 总告警数/确认率/等级饼图/监测点柱状图      |

### 3.4 测试数据生成器

独立 Python 脚本（PyMySQL 直连 MySQL），批量生成模拟历史数据：

- 60 天 × 3 监测点 × 每 5 分钟 1 条 ≈ 5 万条记录
- 每日周期（晨优午差）+ 周周期（工作日人类活动影响）
- 正态分布生成传感器值，3%/5%/8% 概率触发异常
- 综合判定 alert_level + 同步生成 alert_record（推送状态/重试次数/确认时间）
- 500 条/批次 INSERT，完成后输出统计概览

### 3.5 测试体系

| 类型                   | 用例数   | 覆盖内容                                          |
| ---------------------- | -------- | ------------------------------------------------- |
| 边缘端融合算法单元测试 | 6        | 正常/边界/异常输入、时序对齐、置信度计算          |
| 边缘端模型推理集成测试 | 11       | 单/双模型推理、量化处理、降级切换、性能基准       |
| MQTT 模拟器            | 4 场景   | 正常/轻度/中度/重度污染场景模拟                   |
| 云端智能分析单元测试   | 10       | 趋势检测/历史对比/水质预测/融合验证/规则降级      |
| 云端告警推送单元测试   | 9        | 多级推送/联合规则/升级策略/重试机制               |
| 云端 API 集成测试      | 13       | 认证/上报/分析/告警/可视化/缓存端点全链路         |
| E2E 全链路测试         | 10 项    | 编译→启动→健康→API→安全→清理，PowerShell 一键     |
| JMeter 压力测试        | 6 线程组 | 登录/查询/可视化/告警/仪表盘/上报，自动 HTML 报告 |

---

## 四、数据库设计

| 表名                  | 说明                      | 关键字段                                                                                                                                              |
| --------------------- | ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| sys_user              | 系统用户                  | username(UNIQUE), password(BCrypt), role(admin/user/readonly), phone, email, status                                                                   |
| monitoring_point      | 监测点                    | name, longitude/latitude DECIMAL(10,7), device_id(UNIQUE), contact_phone, status                                                                      |
| water_quality_data    | 水质数据（按月RANGE分区） | point_id, timestamp, turbidity_level/ntu, cod_value, ph_value, pollution_types(JSON), alert_level, confidence, final_score, image_score, sensor_score |
| alert_record          | 告警记录                  | point_id, alert_level, alert_type, details(JSON), push_status(pending/sent/confirmed/failed), retry_count                                             |
| user_point_permission | 权限关联                  | user_id + point_id 联合主键（多对多）                                                                                                                 |
| edge_device           | 边缘设备                  | device_sn(UNIQUE), device_type, firmware_version, last_heartbeat, status                                                                              |
| sys_config            | 系统配置                  | config_key(UNIQUE), config_value, is_sensitive                                                                                                        |
| alert_confirmation    | 告警确认                  | alert_id + user_id 联合主键, confirm_time                                                                                                             |

**性能索引**：idx_point_alert_score（趋势查询）、idx_retry_status_time（重试扫描）、idx_device_status_heartbeat（心跳查询）。

---

## 五、系统架构

```
边缘端 (Edge Device: Raspberry Pi 4B / Jetson Nano)
│
├─ 视频采集 ──── OpenCV 224×224 预处理 ───────────┐
├─ 传感器读取 ── Modbus RTU (pH/浊度/COD) ────────┤
│                                                  ▼
│              水质分析主控 (ThreadPoolExecutor)
│              并行推理调度 + 模型降级监控
│                │         │          │
│         ┌──────┘     ┌──┘     └──────┐
│         ▼            ▼               ▼
│    TFLite 推理    多源融合算法      规则引擎
│    MobileNetV2    六步融合          阈值降级
│    U-Net          0.6 + 0.4        兜底方案
│         │            │               │
│         └──────┬─────┘───────────────┘
│                │
│    ┌───────────┼───────────┐
│    ▼           ▼           ▼
│  MQTT        gRPC       SQLite
│  QoS1+TLS   告警ACK     离线缓存
│  三类主题   OTA同步     断点续传
│
├─ MQTT ──── 通配符 /wqi/+/... ─────────────┐
├─ gRPC ──── :9090 Protobuf ────────────────┤
│                                            │
└────────────────────────────────────────    │
                                             │
云端 (Cloud: Java 11 + Spring Boot 2.7)      │
◄────────────────────────────────────────────┘
│
├─ MQTT 订阅器 ──── 消息路由分发
├─ gRPC 服务 ────── 双向流 + 3 一元 RPC
├─ WebSocket ────── /ws/alerts (JWT+心跳)
├─ REST API ─────── 6 控制器
│
├─ 智能分析服务 ─── 融合验证 + 趋势检测 + 历史对比 + 水质预测
│
├─ 数据采集服务 ─── 异步队列(1000) + 批量写入(50条/200ms)
├─ 告警推送服务 ─── 三级推送 + 联合规则 + 指数退避重试
├─ 可视化服务 ──── 缓存三层防护(穿透/击穿/雪崩)
├─ 短信服务 ────── 阿里云+Mock + 熔断器 + 手机号查找链
│
├─ 数据层
│   ├─ MySQL 8.4 (分区表 + 读写分离AOP)
│   ├─ Redis (Cache-aside, 可选)
│   └─ Caffeine (本地缓存, 1000条/5min)
│
├─ 安全层
│   ├─ JWT 黑名单(10000容量)
│   ├─ 限流(全局200+用户20+登录10 rps)
│   ├─ SQL注入/XSS 正则过滤
│   └─ 全链路 TraceId (UUID→MDC)
│
└─ 前端 SPA (Vue 3 + Element Plus + ECharts 5)
    ├─ 登录 → 仪表盘 → 告警管理 → 数据可视化
    ├─ 监测点管理 → 系统监控 → 告警统计
    └─ WebSocket 实时告警弹窗通知
```

---

## 六、核心指标要求

| 指标                     | 要求值                             |
| ------------------------ | ---------------------------------- |
| 边缘端单模型推理超时     | ≤ 200ms                            |
| 融合时间对齐窗口         | 500ms                              |
| 影像 / 传感器融合权重    | 0.6 / 0.4（可配置）                |
| MQTT 心跳间隔 / 重连间隔 | 30s / 10s                          |
| 模型版本检查周期         | 30 分钟                            |
| 模型降级重载周期         | 5 分钟                             |
| 批量写入策略             | 50 条或 200ms                      |
| 写入队列容量             | 1000                               |
| 离线缓存（热区 / 冷区）  | 100 / 500 条                       |
| Redis 缓存 TTL           | 300s ± 60s（随机偏移）             |
| 空值缓存 TTL             | 60s                                |
| JWT 有效期               | 24 小时                            |
| 登录锁定                 | 5 次失败 → 15 分钟                 |
| 接口限流                 | 全局 200 + 用户 20 + 登录 10 req/s |
| 告警重试                 | [1,2,4,8,16] 分钟 × 5 次           |
| 告警升级窗口             | 30 分钟内 ≥ 3 条                   |
| 短信熔断                 | 5 次失败 → 600s 熔断               |
| WebSocket 心跳           | 25s ping / 60s 超时                |
| 离线告警保护期           | 7 天（level ≥ 2）                  |
| 断点续传上限             | 连续 5 次失败暂停                  |
| 数据查询限制             | ≤ 30 天 / ≤ 500 条每页             |

---

## 七、开发阶段计划

**第 1 阶段**
: 系统架构设计 → 数据库 DDL（8 表 + 按月分区）→ Spring Boot 脚手架 → JWT + BCrypt + RBAC 权限体系 → MyBatis Plus 数据访问层 → 边缘端 TFLite 推理框架 + 融合算法

**第 2 阶段**
: MQTT 联调 → WebSocket 推送 → 智能分析增强（趋势检测/历史对比/水质预测）→ 告警推送（多级推送/联合规则/指数退避）→ 缓存三层防护 → 边缘端模型测试

**第 3 阶段**
: gRPC 服务端+客户端（双向流 + 一元 RPC）→ 短信平台对接（阿里云/Mock + 熔断器）→ 断点续传 → 安全加固（黑名单/限流/防暴破/SQL注入/XSS）→ 读写分离 AOP → 结构化日志 + ELK → Vue 3 SPA（7 页面）→ 系统监控仪表盘 → E2E + JMeter 压力测试
