# 《智慧水利应用》功能开发与项目进度报告

**项目名称**：基于轻量化深度学习的浙江河道水质智能监测与预警系统

**版本**：v2.0.0

**报告日期**：2026年5月13日

**完成人**：单智屹

---

## 一、项目概述

本项目构建了一套完整的"边缘-云"协同水质智能监测与预警系统。边缘端（Raspberry Pi 4B / Jetson Nano）负责传感器数据采集和深度学习模型推理（MobileNetV2 + U-Net），通过 MQTT 协议将分析结果上传至云端；云端采用 Java Spring Boot 微服务架构，负责数据融合验证、告警推送、可视化查询等功能。系统覆盖浙江省苕溪、钱塘江、西湖等监测点，实现对浊度、COD（化学需氧量）、pH 值等水质指标的多源数据融合分析与实时预警。

**技术栈**：Java 11 + Spring Boot 2.7.18 + MyBatis Plus 3.5.3.1 + MySQL 8.4 + Redis + MQTT + WebSocket | Python 3.9 + TensorFlow Lite + Modbus RTU + OpenCV

---

## 二、系统架构

```
边缘端 (Python)                         云端 (Java Spring Boot)
+---------------------+                +---------------------------+
| video_capture.py    |   MQTT/TLS     | DataCollectionService     |
| sensor_reader.py    | <-----------> | IntelligentAnalysisService|
| model_loader.py     |   Protobuf     | AlertPushService          |
| fusion_algorithm.py |                | VisualizationService      |
| data_cache.py       |                | AuthService + JWT         |
| mqtt_client.py      |                | WebSocket 实时推送         |
+---------------------+                +---------------------------+
          |                                       |
    SQLite (离线缓存)                    MySQL 8.4 + Redis + Caffeine
```

### 已完成模块清单

| 模块 | 文件数 | 说明 |
|------|--------|------|
| 数据库 DDL | 1 | 8 张表，含分区、索引、外键、预置数据 |
| 云端实体层 | 7 | BaseEntity、User、MonitoringPoint、WaterQualityData、AlertRecord 等 |
| 云端 DTO 层 | 6 | AnalysisResult、AlertResult、QueryParams、Result 统一响应等 |
| 云端 Mapper 层 | 6 | MyBatis Plus BaseMapper + 自定义 SQL |
| 云端 Service 层 | 6 | 数据采集、智能分析、告警推送、可视化、认证、短信 |
| 云端 Controller 层 | 5 | 认证、数据上报、告警查询、监测点管理、可视化 |
| 云端配置层 | 6 | WebMVC、WebSocket、Redis、Caffeine、ThreadPool、MqttSubscriber |
| 云端安全层 | 4 | JWT（HS256 24h）、BCrypt 密码加密、RBAC 权限 |
| 云端异常层 | 3 | GlobalExceptionHandler、BusinessException、ErrorCode |
| 边缘端 Python | 11 | 视频采集、传感器读取、模型推理、融合算法、MQTT 通信、离线缓存 |
| 配置文件 | 4 | application.yml、application-local.yml、pom.xml、config.yaml |

**合计**：云端 Java 48 个源文件 + 边缘端 Python 11 个源文件 + 数据库 1 个 DDL 文件

---

## 三、项目进度表

| 阶段 | 时间 | 主要需求点 | 状态 |
|------|------|-----------|------|
| **第1周** | 5.10 - 5.16 | 系统架构设计、数据库建表、项目脚手架搭建、实体与 DTO 定义、安全认证模块（JWT+BCrypt）、基础 CRUD 接口、边缘端 Python 框架搭建、多源融合算法实现 | ✅ 已完成 |
| **第2周** | 5.17 - 5.23 | MQTT 边缘-云通信联调、WebSocket 告警实时推送、智能分析服务增强、告警规则引擎完善、数据可视化趋势查询、Redis 缓存层接入、边缘端模型推理集成测试、单元测试编写 | 🔲 待开始 |
| **第3周** | 5.24 - 5.30 | 边缘-云全链路联调、短信平台对接与熔断重试、断点续传（离线缓存+补传）、系统性能优化（数据库索引优化、连接池调优）、安全加固（TLS配置、CSRF防护）、日志与监控完善 | 🔲 待开始 |
| **第4周** | 5.31 - 6.6 | 全系统集成测试、压力测试与性能基准、部署文档编写、Docker 容器化配置、异常场景覆盖测试、边缘端设备适配（树莓派/Jetson Nano 实机测试） | 🔲 待开始 |
| **测试与验收** | 6.7 - 6.13 | 功能测试、性能测试、安全测试、用户验收、成果文档整理、答辩准备 | 未开始 |

---

## 四、第1周开发详情（5.10 - 5.16）

### 4.1 完成人

| 姓名 | 本周承担工作 |
|------|-------------|
| 单智屹 | 全部模块——系统架构设计、数据库设计与 DDL、云端 Java 后端全模块（实体/DTO/Mapper/Service/Controller/配置/安全/异常共 48 文件）、边缘端 Python 框架全模块（11 文件）、Maven 项目构建与配置、13 项编译与运行时错误修复、API 接口全量测试 |

### 4.2 本周完成内容

#### （1）数据库设计
- 完成 `schema.sql`（212 行），包含 8 张表：`sys_user`、`monitoring_point`、`water_quality_data`（按月分区）、`alert_record`、`user_point_permission`、`edge_device`、`sys_config`、`alert_confirmation`
- 配置索引 15+ 个，外键约束 6 个，预置 3 个监测点数据（苕溪、钱塘江、西湖）和 1 个管理员账号
- 解决 MySQL 分区表不支持外键约束的兼容性问题

#### （2）云端 Java 后端（48 个源文件）

**实体与数据访问层**
- 定义 7 个 JPA 实体类，使用 MyBatis Plus 自动映射下划线命名
- 编写 6 个 Mapper 接口，包含 5 个自定义 SQL 查询（分点查询、时间范围查询、聚合统计、待重试告警扫描）

**业务服务层**
- **数据采集服务（DataCollectionService）**：异步队列写入模式（LinkedBlockingQueue + 线程池），支持批量插入
- **智能分析服务（IntelligentAnalysisService）**：实现多源数据融合验证算法（影像权重 0.6 + 传感器权重 0.4），规则引擎降级模式（阈值判断），异常等级分级（正常/轻度/中度/重度）
- **告警推送服务（AlertPushService）**：多级告警策略（轻度仅入库，中度入库+WebSocket，重度全渠道含短信），指数退避重试（1/2/4/8/16 分钟），PriorityBlockingQueue 重试队列，定时扫描（5 分钟间隔）
- **可视化服务（VisualizationService）**：ECharts 趋势数据查询，热力图聚合统计，分页历史查询，Redis 缓存旁路模式
- **认证服务（AuthService）**：JWT 令牌生成与验证（HS256、24 小时有效期、滑动刷新），BCrypt 密码加密（cost=10），Redis 会话缓存
- **短信服务（SmsService）**：HTTP 调用，指数退避重试（最多 5 次），熔断器保护（失败阈值 5 次，熔断 10 分钟）

**接口控制器层**
- 5 个 REST Controller，共 10+ 个 API 端点
- 统一响应封装 `Result<T>`（code + message + data + total）
- `@Valid` 参数校验

**安全模块**
- JwtTokenProvider：HMAC-SHA256 签名，支持令牌生成/验证/解析/刷新
- JwtAuthenticationFilter：拦截除 `/api/auth/**` 外的所有请求
- SecurityConfig：BCrypt 密码编码器，RBAC 角色（admin/user/readonly）

**通信模块**
- MqttSubscriber：实现 CommandLineRunner + MqttCallback，支持自动重连（10 秒间隔），通配符主题订阅（`/wqi/+/sensor_data`、`/wqi/+/image_analysis`）
- MqttMessageHandler：Protobuf 消息反序列化，消息分发路由
- AlertWebSocketHandler：WebSocket 实时告警推送（`/ws/alerts`），JWT 子协议认证，心跳检测（30 秒间隔）

**全局异常处理**
- GlobalExceptionHandler：统一异常拦截，BusinessException 业务异常映射
- ErrorCode：8 个错误码定义（0000 成功、1001 参数错误、1002 认证失败等）

**配置层**
- 6 个配置类：WebMVC（CORS + 拦截器）、WebSocket、Redis（条件装配）、Caffeine 本地缓存、ThreadPool（4 个线程池：短信/数据库写入/MQTT/重试）、Maven 构建配置
- 双环境配置：`application.yml`（完整）、`application-local.yml`（仅 MySQL，排除 Redis/MongoDB/WebSocket 自动配置）

#### （3）边缘端 Python 框架（11 个源文件）

- **多源数据融合算法（fusion_algorithm.py）**：与设计报告 5.1 节伪代码一致，时序对齐（500ms 窗口），加权投票（影像 0.6 + 传感器 0.4），置信度计算
- **水质分析主控（water_quality_analyzer.py）**：并行推理调度（ThreadPoolExecutor），模型降级监控（5 分钟轮询），规则引擎回退
- **模型加载器（model_loader.py）**：TensorFlow Lite 单例模式，MobileNetV2 + U-Net 双模型，量化推理支持
- **传感器读取（sensor_reader.py）**：Modbus RTU 协议，pH/浊度/COD 三参数采集
- **视频采集（video_capture.py）**：OpenCV 摄像头帧读取，定时截图
- **MQTT 客户端（mqtt_client.py）**：TLS 加密通信，JSON 序列化，QoS 1
- **本地缓存（data_cache.py）**：LRU 变种（热区 LRU + 冷区 FIFO），SQLite 持久化，断点续传队列
- **告警推送（alert_push.py）**：边端本地告警触发
- **单元测试**：6 个测试用例全部通过（正常融合、传感器缺失、传感器过期、严重告警、正常水质、规则引擎）

#### （4）自动化测试脚本
- 编写 `test-api.ps1` PowerShell 一键测试脚本，覆盖 6 大模块 9 个 API 端点
- 测试结果：全部 `0000 SUCCESS`（登录、监测点查询、4 种告警等级上报、趋势查询、告警记录、分页查询）

### 4.3 本周解决的问题

| 序号 | 问题描述 | 解决方案 |
|------|---------|---------|
| 1 | MySQL 分区表不支持外键约束 | 移除 `water_quality_data` 表的外键，应用层保证数据一致性 |
| 2 | Java 11 缺少 JAXB 导致 `NoClassDefFoundError` | 添加 `javax.xml.bind:jaxb-api:2.3.1` 依赖 |
| 3 | JDBC 不支持 `characterEncoding=utf8mb4` | 修改为 `characterEncoding=UTF-8` |
| 4 | Spring Boot 2.7 + Java 11 `Stream.toList()` 不可用 | 改用 `.collect(Collectors.toList())` |
| 5 | `Map<String, Double>` 类型与 JSON 反序列化不兼容 | 改为 `Map<String, Object>` + `toDouble()` 安全转换 |
| 6 | Redis/MongoDB/WebSocket 未安装导致启动失败 | `@ConditionalOnBean` 条件装配 + `@Autowired(required = false)` |
| 7 | `application-local.yml` 中 `spring.profiles.active` 无效 | 移除该行，通过 CLI `--spring.profiles.active=local` 指定 |
| 8 | MQTT topics 属性缺失导致 `Could not resolve placeholder` | 添加默认值 `@Value("#{'${mqtt.topics:}'.split(',')}")` 和空值跳过逻辑 |
| 9 | MyBatis `@Select` 中 `<foreach>` 未加 `<script>` 导致 500 错误 | 添加 `<script>...</script>` 包裹标签 |
| 10 | `AlertPushService` 中 `(Double) Integer` 强转 `ClassCastException` | 实现 `toDouble(Object)` 安全类型转换方法 |
| 11 | `AlertRecord` 缺少 `@TableId` 导致自增 ID 未回填 | 添加 `@TableId(type = IdType.AUTO)` 注解 |
| 12 | BCrypt 密码哈希与预置数据不匹配 | 使用 Python bcrypt 库重新生成哈希并更新数据库 |
| 13 | Maven 未安装在系统 PATH 中 | 从 VS Code 扩展复制 `maven-wrapper.jar`，配置 `.mvn/wrapper/` |

### 4.4 当前系统运行状态

- **运行环境**：Windows 11，Amazon Corretto JDK 11.0.31，MySQL 8.4
- **启动命令**：`java -jar smart-water-quality-2.0.0.jar --spring.profiles.active=local`
- **启动时间**：~4.1 秒
- **端口**：8080
- **当前可用模块**：认证登录、监测点管理、数据上报与分析、告警记录查询、趋势数据查询、分页历史查询
- **暂未启用模块**：Redis、MongoDB、MQTT Broker、WebSocket（需安装对应中间件后启用）

---

## 五、第2周开发规划（5.17 - 5.23）

### 5.1 需求点与任务拆分

| 序号 | 需求点 | 详细任务 | 预计工时 |
|------|--------|---------|---------|
| 1 | MQTT 边缘-云通信联调 | 搭建本地 MQTT Broker（Mosquitto），配置 TLS 加密；边缘端 MqttClient 与云端 MqttSubscriber 联调；验证 Protobuf 消息序列化/反序列化；通配符主题 `/wqi/{point_id}/sensor_data` 和 `/wqi/{point_id}/image_analysis` 端到端测试 | 2 天 |
| 2 | WebSocket 告警实时推送 | 前端 WebSocket 连接测试（`/ws/alerts`），JWT 子协议认证验证；告警消息广播与指定用户推送功能测试；心跳超时断开与重连机制测试 | 1 天 |
| 3 | 智能分析服务增强 | 完善融合算法参数调优（权重系数、时间窗口）；实现滑动窗口异常趋势检测；历史异常数据回查对比；新增水质等级预测接口 | 1.5 天 |
| 4 | 告警规则引擎完善 | 多参数联合告警规则（浊度+COD+pH 组合判断）；告警升级策略（轻度→中度→重度自动升级）；告警确认与解除接口；告警统计报表接口 | 1.5 天 |
| 5 | Redis 缓存层接入 | 安装配置 Redis，启用 Redis 缓存；实现热点数据缓存策略（监测点信息、用户会话、查询结果）；缓存预热与失效策略；缓存穿透/击穿/雪崩防护 | 1 天 |
| 6 | 边缘端模型推理集成测试 | MobileNetV2 水质分类模型加载与推理；U-Net 污染区域分割模型加载与推理；双模型并行推理性能测试；模型量化推理精度对比；规则引擎降级切换测试 | 1 天 |
| 7 | 单元测试与集成测试编写 | 云端 Service 层单元测试（JUnit 5 + Mockito）；Controller 层集成测试（MockMvc）；边缘端融合算法补充边界测试；API 接口自动化测试脚本完善 | 1 天 |

### 5.2 人员安排

| 姓名 | 第2周计划承担工作 |
|------|------------------|
| 单智屹 | MQTT 联调、WebSocket 推送、智能分析增强、告警规则完善、Redis 接入、边缘端集成测试、测试用例编写 |

### 5.3 第2周预期成果

- MQTT 边缘-云通信链路打通，端到端数据传输延迟 < 500ms
- WebSocket 实时告警推送可用，支持多用户并发连接
- 智能分析融合算法参数优化完成
- 告警规则引擎支持多参数联合判断和自动升级
- Redis 缓存命中率 > 80%
- 边缘端双模型并行推理延迟 < 200ms
- 单元测试覆盖率 > 60%

---

## 六、风险与应对

| 风险项 | 影响 | 应对措施 |
|--------|------|---------|
| 树莓派/Jetson Nano 实机缺失 | 边缘端无法实机部署测试 | 在 PC 上使用模拟传感器数据 + USB 摄像头进行功能验证 |
| MQTT Broker 环境配置复杂 | 通信联调延迟 | 优先使用 Mosquitto Docker 镜像快速部署 |
| TensorFlow Lite 模型量化精度损失 | 推理准确率下降 | 设置精度阈值，低于阈值自动切换为规则引擎模式 |
| Redis/MongoDB 安装配置耗时 | 缓存和文档存储模块延后 | 采用条件装配，无中间件时自动降级为内存模式 |

---

## 附录A：系统 API 接口清单

| 方法 | 路径 | 功能 | 认证 |
|------|------|------|------|
| POST | `/api/auth/login` | 用户登录 | 否 |
| POST | `/api/auth/logout` | 用户登出 | 是 |
| GET | `/api/monitoring-points` | 监测点列表 | 是 |
| POST | `/api/data/report` | 上报水质分析数据 | 是 |
| POST | `/api/data/heartbeat` | 边缘端心跳 | 是 |
| GET | `/api/alerts/point/{pointId}` | 查询监测点告警 | 是 |
| POST | `/api/visualization/trend` | 趋势曲线数据 | 是 |
| POST | `/api/visualization/heatmap` | 热力图数据 | 是 |
| POST | `/api/visualization/query` | 分页历史查询 | 是 |

## 附录B：项目文件结构

```
e:\智慧水利应用\
├── database/
│   └── schema.sql                    # 完整 DDL（8 表 + 分区 + 预置数据）
├── cloud-backend/                    # Java Spring Boot 云端（48 .java 文件）
│   ├── pom.xml
│   ├── test-api.ps1                  # API 一键测试脚本
│   └── src/main/java/com/waterquality/
│       ├── WaterQualityApplication.java
│       ├── config/                   # 6 个配置类
│       ├── constant/                 # ErrorCode 常量
│       ├── controller/               # 5 个 REST 控制器
│       ├── dto/                      # 6 个数据传输对象
│       ├── entity/                   # 7 个实体类
│       ├── enums/                    # 2 个枚举
│       ├── exception/                # 3 个异常处理类
│       ├── mapper/                   # 6 个 MyBatis Mapper
│       ├── mqtt/                     # MQTT 订阅与消息处理
│       ├── security/                 # JWT 认证与安全配置
│       ├── service/                  # 6 个业务服务
│       └── websocket/                # WebSocket 告警推送
├── edge-python/                      # Python 边缘端（11 .py 文件）
│   ├── config/config.yaml
│   ├── requirements.txt
│   ├── src/                         # 10 个源模块
│   └── tests/test_fusion.py         # 6 个单元测试
└── 第13组-《智慧水利应用》详细设计报告.pdf
```
